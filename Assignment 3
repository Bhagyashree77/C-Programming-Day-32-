#include<stdio.h>
#include<conio.h>
int factorial(int n)
{
    if(n==0||n==1)
        return 1;
    else
        return n*factorial(n-1);
}
main()
{
    int n,fact;
    printf("Enter a number to find its factorial: ");
    scanf("%d",&n);
    fact=factorial(n);
    printf("Factorial= %d",fact);
}
